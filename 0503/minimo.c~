#include<stdio.h>
#include<math.h>
#include"../librerie/funzioni.h"

double f(double x) {
return(x+1);
}

int main () {
double a=-2,b=0,err=pow(10,-5);

printf("%.7f\n",bisezione (a,b,err,f));
return 0;
}
